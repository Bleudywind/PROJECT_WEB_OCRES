'use strict';

module.exports = {
  showTextPrompt: function showTextPrompt(message, invalidMessage) {
    var _require = require('inquirer'),
        createPromptModule = _require.createPromptModule;

    return createPromptModule()([{
      type: 'text',
      message: message,
      name: 'answer',
      validate: function validate(value) {
        /* istanbul ignore next */
        var valid = Boolean(value);
        /* istanbul ignore next */

        return valid || invalidMessage;
      }
    }]).then(function (input) {
      return input.answer;
    });
  }
};